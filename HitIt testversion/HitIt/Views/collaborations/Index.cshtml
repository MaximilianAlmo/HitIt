@model IEnumerable<HitIt.Models.collaboration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.genre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.files)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.owners)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.profile.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.genre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.files)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.owners)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.profile.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.name1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.name1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.name1 })
        </td>
    </tr>
}

</table>
